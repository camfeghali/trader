export const config = {
    // WebSocket endpoint URL
    websocketUrl: 'ws://localhost:8000/ws',

    // Number of rows to display in each table
    maxTableRows: 15,

    // Reconnection settings
    reconnectInterval: 5000, // 5 seconds
    maxReconnectAttempts: 5
}; 